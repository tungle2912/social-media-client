/**
 * Define things for client using
 * Client can use browser's functions like window, document, localstorage, session storage, browser cookie, ...
 * Client can not access to server's cookies
 */

export function sampleFunc() {
  // Sample execute logic
}
